# Prediction-of-Hours-spent-in-Discord
“I trained a deep neural network in PyTorch to predict weekly Discord usage hours based on user activity data. The model was exported to ONNX and deployed in the browser using HTML and JavaScript.
